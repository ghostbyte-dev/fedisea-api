services:
  fedisea-api:
    image: ghcr.io/ghostbyte-dev/fedisea-api:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - JAVA_OPTS=-Xss512k -XX:ReservedCodeCacheSize=64M -XX:MaxMetaspaceSize=100M
      - BPL_JVM_THREAD_COUNT=20
    networks:
      - fedisea-network
      - proxy-network
    volumes:
      - fedisea_icons:/app/icons
    restart: always

  icon-server:
    image: nginx:alpine
    container_name: icon-server
    volumes:
      - fedisea_icons:/usr/share/nginx/html:ro
    networks:
      - proxy-network

volumes:
  fedisea_icons:
    driver: local

networks:
  fedisea-network:
    external: true
  proxy-network:
    external: true